table:
  name: users_user
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - name
        - last_seen
      filter:
        id:
          _eq: X-Hasura-User-Id
